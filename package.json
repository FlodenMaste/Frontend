{
  "name": "frontend",
  "version": "1.0.0",
  "scripts": {
    "build": "cd Classroom && npm install && npm run build",
    "start": "cd Classroom && npx serve -H 0.0.0.0 -l 5173 dist"
  },
  "dependencies": {
    "recharts": "^3.3.0"
  }
}
